apiVersion: batch/v1
kind: Job
metadata:
  name: test-health-job
spec:
  template:
    spec:
      containers:
        - name: migration
          image: busybox
          command:
            - sh
            - -c
            - |
              sleep 20
              echo "deploy-job"
